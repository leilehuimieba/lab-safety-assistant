# Proposal

## change 名称
2026-04-14-v8-2-demo-flow-freeze

## 日期
2026-04-14

## 当前状态
active

## 背景 / 问题
- 当前答辩材料已经基本收敛，但“现场到底怎么演、从哪里开始、固定问什么、每一步什么算成功”还没有完全冻结。
- 如果没有一份 `v8.2` 演示主链路冻结文档，现场仍可能出现顺序漂移、临时换题、切页混乱等问题。
- 当前项目优先级是课程 / 大创展示，因此需要把“可复现演示链路”单独固化成主推进项。

## 服务阶段
- Phase 1 - 演示主链路固化

## 目标
- 冻结 `v8.2` 演示主入口和展示顺序
- 固定现场问题集、场景输入和展示目标
- 固定每一步的成功判定和最小保底主线
- 固定异常情况下的备用链路

## 非目标
- 不修改业务代码
- 不新增演示功能
- 不解决 prod / go-live 阻塞
- 不重写所有答辩材料

## 范围
- 包含：
  - `docs/ops/v8_2_demo_flow_freeze_cn.md`
  - `docs/roadmap.md`
  - `docs/changes/`
  - 必要的 `README.md` 文档导航更新
- 不包含：
  - `web_demo` 功能改造
  - 新测试执行
  - 发布链路改造

## 影响面
- 影响模块：演示主线、彩排方式、现场切换顺序
- 影响演示链路：直接影响，是本 change 的核心目标
- 影响发布 / 验证链路：不直接修改，但会规范“展示时引用哪些证据”

## 验收标准
- [ ] 已建立 `v8.2` 演示主链路冻结文档
- [ ] 已冻结主入口、固定问题和固定场景
- [ ] 已明确每一步成功判定
- [ ] 已明确最小保底主线
- [ ] 已明确异常切换和备用链路

## 风险
- 如果后续页面或模块能力变化，冻结文档需要同步维护
- 如果现场条件与当前假设差异较大，仍需一轮彩排验证

## 回退方案
- 回退点：回到以 `demo_freeze_runbook_cn.md` + 脚本为主的方式
- 回退条件：若新的冻结文档与现有答辩总表明显冲突
- 回退步骤：暂停使用本 change 输出，保留此前答辩材料体系

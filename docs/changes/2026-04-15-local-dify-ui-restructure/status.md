# Status

## 当前状态
- active

## 当前步骤
- 已完成 `web_demo` 的第二轮页面工作台化：在原有“侧边栏 + 分区展示”基础上，新增了“本地 Dify / 知识库”独立工作区，并把知识命中抽查能力收进前端；当前继续补一轮以用户体验为中心的前端微调。

## 已完成
- 已确认用户本轮优先诉求是：先优化本地版本，不管服务器，围绕本地 Dify、知识库和前端展示做可交付收敛
- 已建立并切换本次 change 为 active
- 已将页面从单页长页面改造为左侧导航 + 主内容区的工作台结构
- 已拆出 6 个功能视图：总览 / 智能问答 / 准入阻断 / 本地 Dify 与知识库 / 培训与看板 / 事故复盘
- 已将“当前问题”作为跨分区共享上下文，避免用户在不同模块重复输入
- 已新增本地工作区状态接口：输出 Dify 配置状态、连接状态、知识库条数、低置信积压、分类分布和危险源分布
- 已新增“基于当前问题抽查知识命中”的前端入口，方便后续知识库补强
- 已完成代码级验证：FastAPI `TestClient` 可返回 `/api/workspace/status`、`/api/search`，首页 HTML 也已包含新的工作区导航和分区标记
- 已补 Windows 本地标准启动入口：仓库根 `.env.web_demo.example`、`scripts/start_web_demo_local.ps1`、`scripts/stop_web_demo_local.ps1`、`scripts/status_web_demo_local.ps1`、`docs/ops/local_web_demo_windows_quickstart_cn.md`
- 已确认本地 `OPENAI_MODEL=gpt-5.3-codex` 已真正生效，`/api/meta` 返回 `runtime_model = gpt-5.3-codex`
- 已修复 OpenAI 兼容 SSE 的 UTF-8 解码，`mode=agent` 下中文问题不再出现返回内容乱码
- 已补 runtime 记录字段：除 `runtime_model` 外，还同步记录 `app_version`、`acceptance_status`、`chat_lane_agent`、`lab_lane_state`
- 已补本地状态脚本展示项：现在可直接看到 `model / agent_lane / lab_mode / launch_file / err_file`
- 已完成一轮用户体验向前端微调：
  - 已为移动端加入导航折叠按钮，减少首次进入时导航占满整屏的问题
  - 已在主内容顶部新增“当前工作场景 / 推荐下一步”上下文卡片，降低首次进入后的认知负担
  - 已为问答、风险评估、生成清单、提交清单、培训抽题、知识命中抽查、创建复盘等关键操作补统一 loading / busy 文案
  - 已为关键交互元素补 `focus-visible` 高亮，改善键盘与可访问性体验
  - 已为移动端收敛侧边栏冗余说明，只保留必要入口和标题，提升小屏可读性
- 已完成“培训与看板”页信息层级优化：
  - 已把该页明确拆为“培训动作区 + 管理结果区”两列结构，减少培训和管理信息混在一起的感觉
  - 已新增区块标题、说明文案和轻量提示，帮助用户理解这一页该先看什么、后看什么
  - 已将管理端筛选项改为带标签的过滤卡片，而不是直接堆 4 个下拉框
  - 已将“低置信问题 / 最近高风险场景 / 事故复盘摘要”改成更明确的洞察卡片结构
- 已完成同类产品前端参考收敛，并沉淀为 `docs/ops/frontend_benchmark_reference_cn.md`：
  - 已对照 SafetyCulture、EcoOnline、Dify、incident.io、Linear、Notion、Intercom 等产品提炼可借鉴的信息架构与页面组织模式
  - 已明确当前项目更适合继续走“轻量工作台 + 分页分区 + 证据可解释 + 表单减压”的方向
  - 已收敛下一轮最值得做的前端项：优先优化“事故复盘页表单减压”
- 已完成“事故复盘”页轻量重构：
  - 已把原先连续堆叠的长表单改成“核心信息 / 事件时间线 / 原因与改进”三段式
  - 已新增右侧说明与状态区，让录入表单和复盘列表职责分离
  - 已适度借鉴同类安全 / 事故产品的卡片化与时间线组织方式，但仍保持原有接口和字段不变
- 已完成“事故复盘”页浏览器级真实回归：
  - 桌面端已确认 `incident-shell` 双栏布局、3 张亮点卡、2 段时间线、右侧说明区和状态筛选区均正常显示
  - 桌面端已实际创建一条复盘记录，返回 `已创建复盘记录：INC-20260416-0d015c50`，且列表中可看到新建标题
  - 移动端已确认导航折叠、切页和事故复盘页首屏展示正常
- 已完成“知识命中解释层”收敛：
  - 已把“知识命中抽查”从纯结果列表升级为“命中总结 + 补强建议 + 单条解释”的解释层结构
  - 已增加“高相关 / 中相关 / 弱相关”标签、命中原因、相关性判断和下一步使用建议
  - 已完成桌面端与移动端浏览器级回归，确认解释层卡片、逐条解释和命中列表都能正常显示
- 已完成“看板一句话结论层”收敛：
  - 已在管理端看板顶部新增“总体结论 / 当前最需要关注 / 答辩时怎么说”3 张总结卡
  - 已基于真实 dashboard 接口返回动态生成结论，而不是写死口号
  - 已完成桌面端与移动端浏览器级回归，确认总结卡和原有指标区可同时正常显示
- 已完成“低置信标签文案收口”：
  - 已将管理端看板里直接暴露的低置信技术型 reason code 映射成更适合老师/评委理解的展示文案
  - 已保留原始后端 reason code 作为数据层事实，只在前端展示层做人话化收口
  - 已完成浏览器级验证，确认页面不再直接显示 `top_score_below_threshold:0.1<3.5` 这类技术口径

## 未完成
- 尚未完成浏览器层完整交互回归
- 尚未把“本地 Dify 环境优化”拆成独立 change；当前只先把本地 demo 启动入口统一了
- 尚未把“知识库结构与内容优化”拆成独立执行步骤或独立 change
- 已确认当前 8088 运行实例已切换到最新模板与接口版本，`/api/workspace/status` 返回 200
- 当前本机 `127.0.0.1:8080` 仍无本地 Dify，lab 问答仍处于结构化 fallback 模式
- 尚未继续补整站其余页面的浏览器级完整回归；当前重点只补了“事故复盘”页
- 尚未继续补整站其余页面的浏览器级完整回归；当前已重点补 workspace / incident / training 看板核心页

## 阻塞
- 当前无代码级硬阻塞
- 当前验证阻塞：浏览器级完整回归尚未补齐，且本地 Dify 仍未恢复

## 下一步
1. 再决定是否补整站其余页面的浏览器级完整回归
2. 再收敛“本地 Dify 环境优化”任务树，明确本机是否恢复 `127.0.0.1:8080` 的本地 Dify
3. 再收敛“知识库优化”任务树，优先围绕高频演示问题补条目，而不是先大面积重写
4. 如继续做前端，可继续收口高风险记录与事故状态列表的展示话术

## 最近更新时间
- 2026-04-16 12:26

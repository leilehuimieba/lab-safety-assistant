# v8.2 演示链路本地服务/API 预检（2026-04-15）

> 用途：记录一次**本地服务启动 + API 级主链路预检**结果。  
> 说明：本文不是“真实浏览器计时彩排”记录，不能直接替代 `T10-1`，但可作为正式彩排前的技术预检证据。  
> 历史路径说明：文中出现的 `D:\workspace\...` 为本次预检发生时的历史运行路径，仅用于证据追溯；当前默认工作区以 `D:\newwork\lab-safe-assistant-workspace\lab-safe-assistant-github` 为准。  
> 关联 change：`docs/changes/2026-04-14-v8-2-demo-flow-freeze/`

## 1. 预检目的

本次预检主要验证：

1. `web_demo` 本地服务能否正常启动
2. 正式现场默认五步主线对应的关键 API 是否可用
3. 首页是否包含问答、清单、看板、事故复盘、培训模块入口
4. 当前更大的风险是在浏览器现场彩排，还是在服务 / 接口本身

## 2. 预检环境

- 日期：2026-04-15
- 本地地址：`http://127.0.0.1:8088`
- 启动方式：`python -m uvicorn app:app --host 127.0.0.1 --port 8088`
- 服务目录：`D:\workspace\lab-safe-assistant-github\web_demo`
- 说明：以上服务目录为预检发生时的历史执行路径，不是当前默认入口；当前默认以新路径仓库和 `scripts/start_web_demo_local.ps1` 为准。

## 3. 关键结果

### 3.1 服务启动

- 结果：**通过**
- 现象：本地首页返回 `200`

### 3.2 首页模块存在性

- 结果：**通过**
- 已确认页面包含：
  - 演示问题输入
  - 开工前检查清单
  - 管理端看板
  - 事故复盘
  - 培训考核 MVP

### 3.3 问答链路

- 结果：**通过，但当前为结构化回退链路**
- 主问题：`危险实验开始前最关键的阻断项是什么？`
- 返回耗时：约 `34.7 ms`
- 当前返回：
  - `decision = llm_fallback_structured`
  - `model = fallback-rule-engine`
  - 非 Dify 正式链路

说明：
- 当前环境下并未走 Dify 正式知识库工作流，而是走了结构化 fallback。
- 这意味着：**本地服务可演示，但正式答辩若要验证“正式问答链路”，仍需结合现场环境再确认。**

### 3.4 开工前检查清单

- 结果：**通过**
- 场景：`夜间进行高温回流和有机溶剂加热实验`
- 返回：
  - `risk_level = High`
  - `risk_score = 4`
  - 清单项数：`10`
  - 关键项数：`9`

### 3.5 阻断提交

- 结果：**通过**
- 方法：故意留空一个关键项
- 返回：
  - `allow_start = false`
  - 已成功生成阻断原因
  - 已成功生成下一步建议

### 3.6 管理端看板

- 结果：**通过**
- 返回关键指标包括：
  - 清单阻断率：`100%`
  - 培训通过率：`0%`
  - 培训平均分：`0.0`
  - 未闭环复盘数：`0`
  - 逾期整改数：`0`

- 额外观察：
  - 低置信问题列表可返回
  - 最近高风险场景可返回
  - 事故汇总结构可返回

### 3.7 事故复盘列表

- 结果：**接口通过，但当前无记录**
- 返回：
  - `/api/incidents` 正常
  - 当前记录数：`0`

说明：
- 这与当前“事故复盘默认不进正式主线，仅作可选扩展”的收敛方向一致。

## 4. Meta 信息

本次预检得到的关键 meta：

- `app_version = defense-freeze-20260331`
- `acceptance_status = 已封版`
- `formal_eval_score = 20/20`
- `stability_status = 3/3 PASS`
- `knowledge_base_imported = 398`
- `demo_port = 8088`
- `runtime_model = gpt-5.2-codex`
- `chat_lane_lab = Dify 未配置，当前处于结构化回退模式`

## 5. 结论

### 预检结论
- `pass`

### 判定
- 本地 `web_demo` 服务可启动
- 正式现场默认五步主线中，除“浏览器点击式完整彩排”外，关键 API 均已可验证
- 当前最主要未完成项，不再是服务起不来，而是：
  1. **浏览器级真实计时彩排**
  2. **正式环境下问答链路是否仍走 Dify 正式知识库工作流**

## 6. 当前不能直接下结论的部分

以下内容，本次预检**不能**直接证明：

1. 正式现场彩排总时长一定在 4.5 - 5 分钟内
2. 浏览器点击流程一定完全顺滑
3. 正式答辩现场一定走 Dify 正式链路
4. T10-1 已完成

因此：

- 本次仅可视为 **T10-1 的前置技术预检**
- **不能替代真实计时彩排记录**

## 7. 建议下一步

1. 基于 `docs/ops/v8_2_demo_final_freeze_cn.md` 做一轮真实浏览器计时彩排
2. 回填 `docs/ops/v8_2_demo_rehearsal_round1_draft_cn.md`
3. 用 `docs/ops/v8_2_demo_verify_checklist_cn.md` 做勾检
4. 再用 `docs/ops/v8_2_demo_freeze_gate_cn.md` 做最终冻结判断

# v8.2 最终现场冻结版（脚本 + 逐步话术已冻结，待主讲人口播计时盖章）

> 用途：作为当前 **上台展示时的唯一现场口径**。  
> 当前状态：**脚本与逐步话术已冻结，主讲人口播计时 Gate 待盖章**。  
> 若与 `docs/ops/v8_2_demo_flow_freeze_cn.md`、`docs/ops/demo_freeze_runbook_cn.md`、旧答辩脚本冲突，**现场执行以本文为准**。  
> 逐句讲法、点击顺序和备用话术，进一步以 `docs/ops/v8_2_demo_final_script_cn.md` 为准。

## 1. 当前唯一现场主线

### 默认版本
- 默认采用：**4.5 - 5 分钟稳态版**

### 当前唯一现场入口
- **直接从 `web_demo` 开始**
- 地址：`http://127.0.0.1:8088`

### 当前唯一允许的主线顺序
1. 平台总览
2. 智能问答（`lab` 正式链路）
3. 准入阻断
4. 培训与看板
5. 指标收口

## 2. 当前主线冻结依据

基于 2026-04-16 新工作区下的真实浏览器级主线走查，当前口径冻结为：

- 平台总览页可稳定展示 `defense-freeze-20260331`、`已封版 / 20/20`、`398 导入 / 96 本地`、`Dify 正式知识库工作流`
- `lab` 问答可在页面内稳定返回 `链路=dify-workflow`
- 准入阻断页可继承当前问题，并可直接提交出“清单阻断，暂不可开工”
- 培训与看板页可稳定打开，并能展示 `清单阻断率 90% / 培训通过率 0% / 低置信问题 5 条 / 最近高风险场景`
- 事故复盘页虽可打开，但当前无现成记录，**不适合作为默认主线步骤**

因此：
- **事故复盘默认移出主线**
- **README 默认不进主线**
- **后台配置页 / 部署页 / go-live 文档默认不进主线**

## 3. 当前现场默认不进主线的内容

以下内容只作为答辩追问材料，不作为默认展示步骤：

- README 首页
- 本地 Dify / 知识库页
- 事故复盘完整录入流程
- 培训考核抽题与评分全过程
- Dify 后台页面
- 服务器部署页面
- prod / go-live 阻塞说明

## 4. 当前现场成功定义

满足以下 5 条，即视为本轮展示成功：

1. 已完成平台总览
2. 已完成 `lab` 问答，并明确这是知识库正式链路
3. 已完成一次“未满足关键项 -> 暂不可开工”的阻断展示
4. 已完成一次管理看板展示，并指出 4 类核心指标
5. 已完成统一指标收口：`398 / 20/20 / 3/3 PASS`

## 5. 当前冻结结论

在尚未完成主讲人口播计时彩排盖章前，当前统一按以下口径执行：

- 默认版本：**4.5 - 5 分钟稳态版**
- 默认入口：**直接 `web_demo`**
- 默认主线：**平台总览 -> `lab` 问答 -> 准入阻断 -> 培训与看板 -> 指标收口**
- 事故复盘：**默认移出主线，仅作扩展**
- README：**默认移出主线，仅作补充材料**
- 唯一脚本口径：`docs/ops/v8_2_demo_final_script_cn.md`

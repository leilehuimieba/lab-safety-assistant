name: Weekly Report

on:
  schedule:
    # Every Monday 01:00 UTC
    - cron: "0 1 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: read

jobs:
  weekly-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Get Date
        id: date
        run: echo "today=$(date -u +%Y-%m-%d)" >> "$GITHUB_OUTPUT"

      - name: Run Eval Regression Pipeline
        env:
          DIFY_BASE_URL: ${{ secrets.DIFY_BASE_URL }}
          DIFY_APP_API_KEY: ${{ secrets.DIFY_APP_API_KEY }}
        run: |
          python scripts/run_eval_regression_pipeline.py --repo-root . --allow-skip-live --update-dashboard

      - name: Generate Failover Status
        run: |
          python scripts/generate_failover_status.py --repo-root . --days 7

      - name: Generate Weekly Report
        run: |
          python scripts/generate_weekly_team_report.py --repo-root . --since "7 days ago" --until "now"

      - name: Generate Weekly Gate Ops
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python scripts/generate_weekly_gate_ops.py --repo-root . --days 7 --output docs/eval/weekly_gate_ops.md

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          branch: bot/weekly-report
          delete-branch: true
          commit-message: "chore: auto-generate weekly report ${{ steps.date.outputs.today }}"
          title: "chore: weekly report ${{ steps.date.outputs.today }}"
          body: |
            This PR is automatically generated by GitHub Actions.

            - Triggered eval regression pipeline (live run when Dify secrets are configured)
            - Refreshed failover snapshot at `docs/eval/failover_status.md`
            - Generated weekly report under `docs/weekly_reports/`
            - Refreshed eval dashboard under `docs/eval/eval_dashboard.md`
            - Updated gate weekly ops summary at `docs/eval/weekly_gate_ops.md`
            - Please review and merge if content looks correct
          labels: |
            documentation
            weekly-report
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>

